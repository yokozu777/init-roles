# Role 13_configure_repo default variables (override in group_vars/all.yml or host_vars)
cleanup_repositories: false  # Remove all existing repos before configuration when true
install_additional_repos: false  # Configure repos by OS (Ubuntu/Debian/Oracle Linux) when true
